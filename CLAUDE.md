# CLAUDE.md

このファイルは、本リポジトリで作業する Claude Code（claude.ai/code）への
指針を提供します。

## リポジトリの状態

学習プロジェクトの立ち上げ段階。現時点で「正」となるのは `project.txt`
（学習計画と Claude の担当範囲を定義）のみで、実装コードも解説サイト
（`docs/site/`）もまだ存在しない。Git リポジトリも未初期化。

## プロダクト概要

Python のゲームライブラリ **Pyxel** を使って、プロジェクト管理者本人（以下
「ユーザー」）がゲーム制作プログラミングを習得するための **学習プロジェクト**。
Claude は完成版のコードを一方的に渡すのではなく、各 step のゲームをフェーズに
分割し、フェーズごとに「考え方 → 必要な Python／Pyxel API → コード → 解説」を
HTML（`docs/site/`、GitHub 公開）として提示する。ユーザーはそのサイトを見ながら
自分でコードを手入力し、実行確認しながら理解を深める。

### 学習ステップ

| step | 内容 | 備考 |
|---|---|---|
| step0（準備） | Pyxel の環境構築、簡単なアニメーション作品を作って GitHub 公開 | リソースエディタの利用、画面表示（テキスト・スプライト）、スプライトに動きをつける、BGM 操作、GitHub 公開を広く浅く学ぶ |
| step1 | 「お化けに捕まらずにどれだけお菓子を集められるか」ゲーム | ステージ等はなし。画面遷移（タイトル／ゲーム／ゲームオーバー）あり。参考：https://qiita.com/rwatanab1999/items/d5c0bb876f0b44cac2f0 |
| step2 | 「倉庫番」 | 参考記事にはステージがないので、10 ステージ制とタイトル画面を追加して拡張する。参考：https://note.com/syun77/n/nd7675f2df64b |
| step3 | 縦スクロールシューティング | 3 ステージ制。中ボス・ボスが登場。弾幕やパワーアップアイテム等 |
| step4 | 未定 | step3 まで完了してからジャンル・内容を検討する |

### Claude の担当範囲

1. step ごとの **完成イメージ** と、完成版に必要な **プログラミング技術項目** を
   洗い出す（解説サイトにも掲載する）
2. 完成版をいくつかの **フェーズに分割** し、フェーズごとに考え方・必要な Python
   コマンド・Pyxel API コマンド等のコードを作成する（コードが 1 つか複数かは
   フェーズ内容による）
3. 2 で挙げたコードを、**コードごとに解説を付けて HTML 化** して提示する
4. Python や Pyxel、サンプルコードについてユーザーから出た質問は、3 のサイトに
   **備忘録として蓄積** する

### 厳守すべき制約

- **完成版のコードをいきなり提示しない。** ユーザーは完成版を見ても理解できない
  ため、必ずフェーズ分割して段階的に提示する
- コードは **ユーザーが手入力する** 前提。写経しやすい分量に区切り、Claude が
  実装を先回りして完成させない
- ユーザーの Python 理解度の前提：インストールや pip の使い方、順次・反復・分岐、
  文字列と数値の違い、モジュール、リストなどを使った基本的なコードは理解できる。
  これを超える概念が出てきたら都度解説する
- 解説 HTML の保管場所は `docs/site/` フォルダ（GitHub にて公開）
- 質問への回答はその場限りにせず、必ず `docs/site/` の備忘録へ残す
- 著作権に配慮し、参考 URL の記事や既存ゲームの画像素材は転載しない

### 完了条件

1. step ごとの完成イメージと必要な技術項目が `docs/site/` に掲載されている
2. 各フェーズのコードをユーザーが手入力して実行し、想定どおり動作することを
   確認できている
3. ユーザーからの質問が `docs/site/` の備忘録へ反映されている
4. step0 の作品および各 step の成果物が GitHub で公開されている

## 使用言語

本プロジェクト配下での生成テキストはすべて日本語で書く。対象は以下を含む。

- ユーザーへのチャット返信、進捗報告、ターン末の要約
- `AskUserQuestion` の質問文・選択肢のラベルと説明
- コード内のコメント（`//`, `#`, `/* */`, docstring 等）
- コミットメッセージ、PR タイトル、PR 本文
- README、CLAUDE.md、設計メモ、その他のドキュメント類
- エラーメッセージやログ出力のうち、エンドユーザー向けに表示される文字列

例外として **英語のまま** とするのは次のみ（言語仕様・エコシステム慣習のため）。

- プログラミング言語のキーワード、標準ライブラリ／フレームワーク／パッケージの API 名
- 識別子（変数名・関数名・クラス名・ファイル名・ディレクトリ名など）
- 設定ファイルのキー名、環境変数名

## 確定済みの実装方針

- 言語／ライブラリ：Python + Pyxel（レトロゲーム用ライブラリ）。グラフィック・
  サウンド素材は Pyxel 付属のリソースエディタで作成する
- 実行形態：ローカル実行が基本。Web 公開は Pyxel の配布機能
  （`pyxel package` → `pyxel app2html`）を用いる
- ドキュメント：`docs/site/` に静的 HTML を置き、GitHub で公開する。文字だけに
  せず、画面キャプチャや概念図を併載する
- リポジトリ：現時点では Git 未初期化。GitHub 公開を前提とするため、個人運用
  ファイル（`.todo/`、`.claude/settings.local.json`）は追跡しない
- 学習の進め方：1 フェーズ = 「考え方 → 必要な API → コード → 解説」を 1 セットと
  し、ユーザーの実行確認が済んでから次フェーズへ進む
